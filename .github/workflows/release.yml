name: Validate owner-built release
on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: Release tag with an owner-built VGMissionJournal.zip attached
        required: true
        type: string
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-latest
    env:
      TAG: ${{ github.event.release.tag_name || inputs.tag }}
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          ref: ${{ env.TAG }}
      - name: Download owner-built artifact, never game references
        run: gh release download "$TAG" --pattern 'VGMissionJournal.zip' --dir dist
      - name: Validate file layout
        run: python3 tools/package.py --check dist/VGMissionJournal.zip
